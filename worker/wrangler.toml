name = "rebelroot-backend"
main = "src/index.ts"
compatibility_date = "2026-06-29"

[vars]
# Configure non-sensitive environment settings here if needed
# Sensitive tokens must be uploaded using wrangler secrets:
# $ wrangler secret put TELEGRAM_BOT_TOKEN
# $ wrangler secret put TELEGRAM_CHAT_ID
# $ wrangler secret put DISCORD_WEBHOOK_URL
